namespace CodeQuery.Postman.UI.Models;

public enum TypeEnum { File, Text };

public enum Mode { Formdata, Raw, Urlencoded };

public enum PostmanPreviewlanguage { Json };

public enum Key { Accept, BaseUrl, ContentType };

public enum HeaderValue { ApplicationJson, Empty, MultipartFormData, TextPlain };

public enum Method { Get, Post, Put };

public enum Host { BaseUrl };

public enum ValueValue { FirstNames, String, Surname, TestIdNumber, Uuid, Xxx };

public enum Name { BadRequest, NoContent, NotFound, Ok, Success, Unauthorized };